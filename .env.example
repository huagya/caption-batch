# Copy to .env (never commit .env)
# If you ever pasted keys in chat, rotate them.
GEMINI_API_KEY=
OPENROUTER_API_KEY=
# Optional OpenRouter headers
OPENROUTER_HTTP_REFERER=https://github.com/huagya/caption-batch
OPENROUTER_APP_TITLE=caption-batch
